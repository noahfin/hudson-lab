var prevRow = $('#contact_<%= @contact.id %>').prev()[0];
$('#contact_<%= @contact.id %>').remove();
 $('<%= escape_javascript render(@contacts) %>').insertAfter(prevRow);
$('#contact-form-wiz').hide();
$('#form-modal').modal('hide');
$('#form-modal').removeClass('modal');
$('.select_u_picker').attr("data-actions-box", "true");
      $(function() {
       $.notify({
      icon: 'fa fa-bell',
      title: '<strong>Heads up!</strong>',
      message: 'Contact Got Updated!'
    },{
      type: 'success'



    });

setTimeout(function() {
  $.notifyClose();
}, 2000);

}
