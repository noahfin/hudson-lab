$('#contacts').html('<%= escape_javascript render(@contacts) %>');
$('#pagin').html('<%= escape_javascript paginate(@contacts, remote: true, params: {contact: nil, new_group: nil}.merge(previous_query_string)) %>');
$('#sidenav').html('<%= escape_javascript render("layouts/sidenav") %>');
