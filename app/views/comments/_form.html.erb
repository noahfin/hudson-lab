
<%= simple_form_for(@comment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :body %>
    <%= f.input :–skip-stylesheets %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
