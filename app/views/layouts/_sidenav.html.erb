<div class="card card-5 mt-4" >
  <div class="card-header card-header-icon b-bottom pb-3 mt-4 pt-3">
      <a class="text-info" data-toggle="collapse" href="#collapseGroupSearch" aria-expanded="false" aria-controls="collapseGroupSearch">

    <div class="card-icon bg-dark  ml-2">
      <i class="material-icons">group</i>
    </div>
  </a>
    <div class="pt-3">
      <%= link_to 'Groups ','/groups/', class: "text-info mt-4", remote: true %> | <%= link_to '+ Contact', new_contact_path, class: "text-info  mt-4", remote: true %>
    </div>
    <div class="row m-0 p-0 search-row">
      <div class="collapse m-0 p-0" id="collapseGroupSearch">

<form class="group_search" id="group_search" action="/groups/search" accept-charset="UTF-8" data-remote="true" method="post"><input name="utf8" value="✓" type="hidden"><input name="authenticity_token" value="3hkDkSwcpSd5NTIQf8meRk82+te+Iq232tuP7i0/n9ZxbAdtZORuW/Zmhbtpun/QjA1pzmHuljsMuhE77S6vzQ==" type="hidden">
    <span>
         <span class="bmd-form-group bmd-form-group-lg"><input name="query" id="query" class="form-control form-control-lg form-control-gray bg-gray mr-3 text-white query-c ui-autocomplete-input" placeholder="Name" autocomplete="off" type="text"></span>
        <button type="submit" class="btn btn-outline-info btn-round btn-just-icon" id="send-search">
          <i class="material-icons">search</i>
          <div class="ripple-container"></div>
        </button>
    </span>
</form>
    </div>

  </div>
  </div>
  <div class="card-body sidnav-section" id="body-sidnav">
    <div class="list-group border-0 pt-0  mt-0">
      <% user_contact = current_user.contacts %>
      <% active = params[:group_id] == nil || params[:group_id].empty? && !session[:selected_group_id] ? 'bg-dark' : '' %>
        <%= link_to( contacts_path, class: "list-group-item text-white get-group custom-tip #{active}", data: { data_toggle: 'tooltip', placement: 'right', remote: true },  'aria-expanded': 'false',:title => user_contact.count.to_s + ' Contacts in this Group', rel: 'tooltip'  ) do %>
          All Contacts
      <% end %>

      <% @groups.each do |group| %>
        <% active = params[:group_id].to_i == group.id || (session[:selected_group_id] && session[:selected_group_id].to_i == group.id) ? 'bg-dark' : ''  %>
        <%= link_to( contacts_path(group_id: group.id), class: "list-group-item text-white pt-2 get-group custom-tip #{active}", data: { data_toggle: 'tooltip', placement: 'right', remote: true },  'aria-expanded': 'false',:title => group.contacts.count.to_s + ' Contacts in this Group', rel: 'tooltip' ) do %>
            <%= group.name %>
           <%= hidden_field(:contact, :group_id, :value => group.id.to_s, class: 'get-group-id') %>
          <% end %>
      <% end %>
      <a href="#top" title="" id="back_up" class="pull-right text-info">Back Up</a>
    </div>
  </div>
</div>


