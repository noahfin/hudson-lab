<div class="main-panel">
  <nav class="navbar navbar-expand-lg navbar-transparent  navbar-absolute fixed-top pt-2">
    <div class="container-fluid">
      <div class="navbar-wrapper">
        <div class="navbar-minimize">
          <button id="minimizeSidebar" class="btn btn-just-icon bg-black btn-fab btn-round">
            <i class="material-icons text_align-center visible-on-sidebar-regular">more_vert</i>
            <i class="material-icons design_bullet-list-67 visible-on-sidebar-mini">view_list</i>
          </button>
        </div>
        <a class="navbar-brand text-white " href="/dashboard" id="hudson-lab">Hudson-lab</a>
      </div>
      <button class="navbar-toggler nav-toggler-side-nav" type="button" data-toggle="collapse" data-target="#navigation" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
        <span class="sr-only">Toggle navigation</span>
        <span class="navbar-toggler-icon icon-bar"></span>
        <span class="navbar-toggler-icon icon-bar"></span>
        <span class="navbar-toggler-icon icon-bar"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navigation">
        <%= render 'layouts/searchform' %>
        <ul class="navbar-nav">
          <li class="nav-item <%= active_item('dashboard') %>">
          <%= link_to "<i class='material-icons text-gray'>dashboard</i><p><span class='d-lg-none d-md-block'>Stats</span></p>".html_safe, contacts_path, class: 'nav-link' %>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link text-gray" href="http://example.com" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="material-icons text-gray">notifications</i>
              <span class="notification"><%= @notifications.count %></span>
              <p>
                <span class="d-lg-none d-md-block">Some Actions</span>
              </p>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <% @notifications.each do |notification| %>
                <%= link_to notification_path(notification), class: 'dropdown-item' do %>
                <%= notification.name %>
                <% end %>
              <% end %>

            </div>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link "  id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="material-icons text-gray">person</i>
              <p>
                <span class="d-lg-none d-md-block">Some Actions</span>
              </p>
            </a>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
              <%= render 'layouts/nav_links_for_auth' %>
            </div>
          </li>
        </ul>
      </div>
    </div>
  </nav>


